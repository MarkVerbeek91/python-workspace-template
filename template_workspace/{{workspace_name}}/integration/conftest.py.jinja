# content of conftest.py

import pytest


def pytest_collection_modifyitems(items):
    for item in items:
        if "cc" in item.nodeid:
            item.add_marker(pytest.mark.component)
        elif "sy" in item.nodeid:
            item.add_marker(pytest.mark.integration)